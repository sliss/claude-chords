# Per-user runtime state
state/registry.json
state/sessions/
state/cache/
state/fallbacks.json
state/push_fanfare.enabled
state/leitmotifs.jsonl

# Rendered (path-baked) skill — regenerated by install.py at install time
# from skill/SKILL.md.tmpl. The committed source is the .tmpl.
skill/SKILL.md

# Python
__pycache__/
*.pyc

# macOS
.DS_Store

# Debug
/tmp/claude-chords-debug.log

# Composition output: commit .mid files (canonical), regenerate .wav locally
compositions/output/*.wav
